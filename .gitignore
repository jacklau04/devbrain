*.pglite
.DS_Store
# nightshift runtime: dashboard status.json, served index.html, worker logs, pytest venv
.nightshift/
# scratch artifacts (dogfood screenshots, throwaway evidence) — attach to PRs, never commit
.context/

# python bytecode
__pycache__/
*.pyc
.gstack/

# go build artifact (repo-root binary used by the parity suite)
/devbrain
dist/
